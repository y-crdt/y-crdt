[workspace]

members = [
  "yrs",
  "ywasm",
  "yffi"
]

[profile.release]
# optimization over all codebase ( better optimization, slower build )
codegen-units = 1
opt-level = 3 
lto = true
panic = 'abort'
